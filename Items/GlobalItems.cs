using OmoriMod.Items.TestingItems;
using Terraria;
using Terraria.ModLoader;

// keep imports!

namespace OmoriMod.Items
{
    public class GlobalItems : GlobalItem
    {
        /*
        public override void UpdateInventory(Item item, Player player)
        {
            // An example of how to change an item depending on which bosses have
            // Been defeated. Notice, there will be 'gaslighting' as I do not know
            // How to change default damage or other stuff displayed in crafting
            // That is if this item is craftable.

            // Update: use modifyDamage!
            
            if (DownedBosses.downedSprout)
            {
                if (item.type == ModContent.ItemType<TestBow>())
                {
                    item.damage = 20;
                }
            }
            else if (!DownedBosses.downedSprout)
            {
                if (item.type == ModContent.ItemType<TestBow>())
                {
                    item.damage = 10;
                }
            }
            
        }

        */
        
    }
}
